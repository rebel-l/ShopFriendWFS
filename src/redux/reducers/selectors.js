/**
 * getList returns the shopping list items from the store.
 *
 * @param store
 * @returns {function(*=, *): *[]}
 */
export const getList = store => store.shopList; // TODO: where to put this?

// TODO: what do we need that for? Shouldn't it be part of the Frontend Model?
